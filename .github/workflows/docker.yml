name: Docker

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

concurrency:
  group: docker-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # Builds each platform on its own native runner, in parallel, instead of
  # one job emulating arm64 under QEMU on an amd64 runner. Each job pushes
  # its own arch-suffixed tags (e.g. `0.6.3-amd64`) the moment it finishes,
  # so hosted prod (amd64-only) can deploy right away without waiting on
  # the slower arm64 build or the merged multi-arch manifest below.
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            platform: linux/amd64
            runner: ubuntu-latest
          - arch: arm64
            platform: linux/arm64
            runner: ubuntu-24.04-arm

    runs-on: ${{ matrix.runner }}

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          # Canonical tag semantics (applied to the merged multi-arch
          # manifest by the `merge` job below):
          #   - `latest`       → newest stable release (v* tag push)
          #   - `X.Y.Z` / `X.Y` → specific release versions
          #   - `dev`          → rolling build from `main` (unstable, opt-in)
          # `main` is a rolling integration branch, so it MUST NOT overwrite `latest`.
          # The `sha` line is a fallback for manual `workflow_dispatch` runs
          # (e.g. testing this workflow on a feature branch) so metadata-action
          # always emits at least one tag even when none of the canonical
          # rules above match the ref. It never fires on push/tag events.
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}
            type=raw,value=dev,enable=${{ github.ref == 'refs/heads/main' }}
            type=sha,format=short,enable=${{ github.event_name == 'workflow_dispatch' }}

      - name: Compute arch-suffixed tags
        id: arch_tags
        run: |
          {
            echo 'tags<<EOF'
            while IFS= read -r tag; do
              [ -n "$tag" ] && echo "${tag}-${{ matrix.arch }}"
            done <<< "${{ steps.meta.outputs.tags }}"
            echo EOF
          } >> "$GITHUB_OUTPUT"

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: ${{ matrix.platform }}
          push: true
          tags: ${{ steps.arch_tags.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.arch }}
          cache-to: type=gha,mode=max,scope=${{ matrix.arch }}
          # PostHog source-map upload. Empty on forks / other repos (no
          # access to these secrets) -- the guarded Dockerfile step and
          # next.config.ts's productionBrowserSourceMaps gate both no-op
          # on empty strings, so this is safe to leave unconditional.
          build-args: |
            POSTHOG_CLI_API_KEY=${{ secrets.POSTHOG_CLI_API_KEY }}
            POSTHOG_CLI_PROJECT_ID=${{ secrets.POSTHOG_CLI_PROJECT_ID }}

  # Combines the amd64 + arm64 images built above into a single
  # multi-arch manifest list under the canonical tags (`latest`,
  # `X.Y.Z`, `X.Y`, `dev`). This is what self-host `docker-compose.yml`
  # pulls, and it necessarily waits on both platforms -- the
  # arch-suffixed tags above are the fast path for anyone who only
  # needs one architecture.
  merge:
    needs: build
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}
            type=raw,value=dev,enable=${{ github.ref == 'refs/heads/main' }}
            type=sha,format=short,enable=${{ github.event_name == 'workflow_dispatch' }}

      - name: Create and push multi-arch manifest
        run: |
          # Any one canonical tag works as the source reference -- they
          # all point at the same per-arch build. Use the first.
          source_tag="$(head -n1 <<< "${{ steps.meta.outputs.tags }}")"

          dest_flags=()
          while IFS= read -r tag; do
            [ -n "$tag" ] && dest_flags+=(-t "$tag")
          done <<< "${{ steps.meta.outputs.tags }}"

          docker buildx imagetools create "${dest_flags[@]}" \
            "${source_tag}-amd64" \
            "${source_tag}-arm64"

      - name: Inspect image
        run: |
          # Reuse the tag docker/metadata-action already computed
          # (steps.meta.outputs.tags), which it lowercases internally,
          # instead of re-deriving the reference from `github.repository`
          # here. `github.repository` preserves the original owner/repo
          # case, and registries require a lowercase reference -- an
          # owner name with any uppercase letter (as on a personal fork)
          # made this step fail with "must be lowercase" even though the
          # push itself (which goes through metadata-action's tags)
          # succeeded.
          image_tag="$(head -n1 <<< "${{ steps.meta.outputs.tags }}")"
          docker buildx imagetools inspect "$image_tag"
