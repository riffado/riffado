# 简体中文界面维护

界面固定使用 `zh-CN`。英文源文案保留在组件的 `uiText("English source")` 调用中，中文集中在 `src/lib/i18n/zh-CN.json`。未收录的英文会原样显示，避免上游新增文案导致页面不可用。不需要数据库迁移或新增部署变量。

## 当前范围

登录、注册、密码找回、工作台、录音列表与播放、转写与摘要、设置、引导、Plaud 连接、确认框、快捷键、通知及常见错误提示。日期使用中文显示，ISO 输出保留原样。

官网营销、法律条款、文档、管理后台、计费专属页面、开发工具和邮件正文不在本次范围。品牌、技术标识、URL、配置示例和用户自定义内容保留原文。未知外部服务错误仍保留诊断原文。

## 改动边界

- `uiText` 仅用于应用自有的显示文案，不用于表单值、状态枚举、接口字段、数据库内容或用户输入。
- 动态句子使用完整词条与命名参数，如 `uiText("{count} recordings", { count })`，不直接拼接英文单复数。
- `presets.ts` 只复制并翻译预设的名称、说明和语言选项标签。原始 AI 提示词、语言代码、预设 ID 不变。
- `uiError` 只在显示层翻译已知消息和错误码。API 响应、日志和未知错误原文不变。

## 同步上游

1. 按仓库既有分支流程合并上游更新，保留独立词典及显示层调用。业务冲突按上游行为解决，再恢复对应显示文案的翻译调用。
2. 执行 `pnpm i18n:check`。它检查词典占位符、静态翻译调用缺失词条，以及覆盖目录内直接写在 JSX 和可见属性中的英文。品牌和技术示例使用按文件、按原文匹配的 `allowlist.json`，新增例外需说明理由。
3. 上游修改英文时同步更新调用和词条。不要为了复用旧翻译而保留语义已经变化的旧文案。
4. 人工检查动态模板、对象配置、服务器返回文案和新增目录；静态扫描不能保证发现所有漏译。新增功能目录需加入检查范围。
5. 运行 `pnpm format-and-lint:fix`、`pnpm type-check`、`pnpm test`，再验收桌面与窄屏下的菜单、弹窗、长文案和省略提示。

独立词典能减少合并冲突，但无法保证任意上游重构后自动保持完整汉化。合并后应把漏译检查与人工界面验收作为固定步骤。不要通过全局 DOM 替换、翻译 API 响应或改变 AI 提示词来补漏。
